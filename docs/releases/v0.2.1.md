# TaskDock 0.2.1

本次更新重点改善窗口识别和底部避让体验。

- 自动过滤保存、选色等模态弹窗和内部工具窗口，不再把它们显示为普通任务项。
- 最大化及左右分屏窗口会为 TaskDock 留出底部空间，避免内容被遮挡。
- 缩短底部预留高度，并将窗口与 TaskDock 的间隔调整为 2px。
- 降低重复窗口刷新和面板重绘，改善明显闪烁问题。
- 隐藏、退出或移动 TaskDock 后，自动恢复由 TaskDock 调整过的窗口尺寸。

系统要求：macOS 13 或更高版本，Apple Silicon（arm64）。
